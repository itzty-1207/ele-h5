1. 同源政策
- 协议、端口、域名

2. 限制跨域
- 限制脚本内发起的跨源HTTP请求，XHR活Fetch API
- 只能加载应用的同一个请求HTTP资源

3. 解决跨域
- jsonp <script>标签不受同源策略限制
- 跨域资源共享CORS，允许服务器进行跨源访问
- 使不同源变成同源，反向代理

4. 反向代理
- 正向代理：客户端告诉代理服务器资源地址
- 反向代理：客户端只告诉服务器想要什么资源